;including modules 
(include "./widgets.yuck")

(defwindow bar
  :monitor 0
  :windowtype "dock"
  :hexpand "false"
  :vexpand "false"
  :geometry (geometry
    :anchor "top center"
    :x "0"
    :y "5"
    :width "98.8%"
    :height "4%"
  )
  :stacking "fg"
  :exclusive "true"
  :focusable "false"

  (centerbox
    :class "base_div"
    :orientation "h"
    :spacing 10

    (box
      :class "left"
      :halign "start"
      :space-evenly false
      :spacing 10
      (Arch_logo)
      (workspace)
    )

    (box
      :class "center"
      :halign "center"
      ;"pass"
    )

    (box
      :class "right"
      :halign "end"
      :space-evenly false
      :spacing 8
      (cpu_mem)
      (updates)
      (wifi)
      (battery)
      (time)
    )
  )
)



(defwindow low_batt_warning
  :monitor 0
  :windowtype "dialog"
  :geometry (geometry
    :anchor "center"
    :width "200px"
    :height "20px"
  )
  :stacking "overlay"
  :focusable "true"
  (low_batt_warning)
)
